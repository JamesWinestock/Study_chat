<h3>Comments</h3>
<% commentable.comments.each do |comment| %>
      <p>Posted by: <%= link_to comment.user.username, profile_path(comment.user.profile) %></p>
  <p><%= comment.body %> <%= time_ago_in_words(comment.created_at) + " ago"%> </p>
<% end %>
